missing .ocx index snapshot that support resovling images against a locked index (should set OCX_FROZEN)
maybe provide convenient function to add target/ctest to update a frozen index
should we support a script mode exeuction to update ocx.cmake and Findocx.cmake? Should that support a mirror?